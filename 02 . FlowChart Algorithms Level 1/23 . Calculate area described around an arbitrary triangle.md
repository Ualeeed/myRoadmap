---
DONE: true
DATE: 2025-08-20T17:31:00
related: "[[23 . calculate circle area described around an arbitrary triangle]]"
---
# Requirement : 

write a Flowchart program to : 

calculate circle area described around an arbitrary tringle  then print it on the screen.

$$
p = \frac{a + b + c}{2} \
$$
$$


 Area= \pi .(\frac{a \cdot b \cdot c}{4 \cdot \sqrt{p \cdot (p - a) \cdot (p - b) \cdot (p - c)}} )^2



$$

the user should enter 
- a
- b
- c 

p = (a + b + c) / 2 
T = (a * b * c) / ( 4 * sqrt ( p * (p - a) * (p - b) * (p - c))

# Solution : 

```mermaid
graph TD

    A(Start) --> B[/Read a, b, c/]

    B --> C[Pi = 3.14]

    C --> D[p = a + b + c / 2]

    D --> E[T = a * b * c / 4 * sqrt p * p - a * p - b * p - c]

    E --> F[T = T * T]

    F --> G[Area = Pi * T]

    G --> H[/Print Area/]

    H --> I(End)
```
